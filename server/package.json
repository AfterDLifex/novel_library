{
  "name": "novel-library-proxy-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Local backend proxy that searches novel websites server-side, bypassing browser CORS and bot-blocking issues.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
